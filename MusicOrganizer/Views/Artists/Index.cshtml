@using MusicOrganizer.Models;
@{
  Layout = "_Layout";
}
@if (@Model.Count == 0)
{
  <h1>Sorry, no artists yet!</h1>
}
else
{
  <ol>
  @foreach (Artists artist in Model)
    {
      <li>
        <h2><a href="/artists/@artist.Id">@artist.ArtistName</a></h2>
      </li>
    }
  </ol>
}
