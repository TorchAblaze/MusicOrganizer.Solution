@using MusicOrganizer.Models;
@{
  Layout = "_Layout";
}
@if (@Model.Count == 0)
{
  <h1>Sorry, no artists yet!</h1>
}
else
{
  @foreach (Artists artist in Model)
    {
        <a href="/artists/@artist.Id"><button class="btn btn-outline-info btn-sm">@artist.ArtistName</button></a>
    }
}
